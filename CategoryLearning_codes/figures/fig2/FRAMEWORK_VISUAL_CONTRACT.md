# Fig2a mechanism demonstration

Purpose: show how finite candidate rules yield different predictions for a shared stimulus, how feedback changes their support/precision, and how search can replace a candidate. Schematic-led composite, Python/matplotlib, native artists, 164 × 74 mm in full figure, PNG-only.

Use an explicitly illustrative binary-task episode, NOT participant data or fitted parameters. Three active rule icons project onto F1/F3: F3 threshold, F1 threshold, F1+F3 threshold. A common stimulus (F1=.25,F3=.75) makes these rules disagree. Demonstrate choice 2 and error feedback. Compute before/after beliefs and rule precision using manuscript equations, with fixed declared illustrative values. Mixture and persistent readout are alternatives, not parallel cognitive steps. A subsequent replacement is one possible search realization, not a necessary outcome of one error; executed h1 is retained in this illustration. Proposed h4 is a nonactive rule, and no post-transport probability is fabricated. Search-space positions are schematic, not measured pairwise distances. Global search covers inactive rules; local mass aggregates contributions from all active rules. Curves in the precision inset are rule emission functions, not learning data. Numerical PF averaging is stated briefly outside the cognitive sequence.

Evidence integrity: no empirical panel changed, no fit or simulation run; only analytic schematic quantities are generated. Record the illustrative parameters/probabilities in figure output. Keep equations in the caption/documentation and visual effects in the diagram.
