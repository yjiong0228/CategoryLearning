# 2026-09-21：每个条件再运行一人的完整拟合

按用户最新指示，本轮仅运行S104、S215、S328，暂不启动全体。
使用现有默认v2入口及原配置，不调整认知机制、参数范围、基础种子、评分预算或停止规则。

## 选人方式与范围

沿用上一轮按试次长度选人的规则：每个condition取距离全组中位数最近的候选，距离相同时取编号较小者。
排除已有九份完整v2拟合，以及上一轮协议列出的近期搜索/数值诊断、恢复模板及反事实检查对象。
选人只读取被试编号、condition、试次数与既往参与名单，不读取拟合成绩。
排除清单、候选排序和数据指纹保存在计算前协议中。

| condition | 被试 | 完整试次 | 计分试次 | 全组试次中位数 |
|---|---|---:|---:|---:|
| 1 | S104 | 192 | 191 | 256 |
| 2 | S215 | 768 | 767 | 832 |
| 3 | S328 | 704 | 703 | 672 |

合计1664个完整试次、1661个计分试次；首试次仍参与递推。
这三人在已知近期校准名单之外，不代表从未在任何历史工作中被查看；完整序列NLL仍是样本内成绩。
本轮不以拟合通过为选人或追加计算的条件，不替换不确定个案。

## 固定运行方式

直接使用 `configs/exp123/specific_models/model_0826_adaptive_fit.yaml`。
流程见[默认拟合说明](ADAPTIVE_FIT.md)，规则与[九人之后的工作方案](COHORT_FITTING.md)一致：
45个分散起点，局部/整块/跨块联合搜索；每循环至多4轮、至多2循环；独立审查从R128×B16按原条件升到R256×B32；
平均NLL容差.005、bootstrap 6000，原共同支持不扩展。纯评分不确定到顶保留标记。

每人最多9252次完整PF，合计最多27756次；这是任务量上限，不是必须算满或墙钟时限。
使用一个共享的128-worker池、每worker一个数值线程，按可用CPU和就绪任务取小，不预留核。
先检查三人的完整计划，再分别用32试次、单进程小预算核查数据入口和结果校验器，随后才启动完整作业。

```bash
env PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  NUMEXPR_NUM_THREADS=1 NUMBA_CACHE_DIR=/tmp/model0826_three_subject_numba \
  python -m src.Bayesian_state.run_model_0826_fit \
  --config configs/exp123/specific_models/model_0826_adaptive_fit.yaml \
  --subjects 104 215 328 \
  --output-dir results/model_0826/three_subject_fit_20260921/full
```

本次由持久后台驱动器执行该命令，勿另开同目录重复作业；进度与日志见[本轮记录](../../../results/model_0826/three_subject_fit_20260921/README.md)。
驱动器完成后核对全部PF缓存的身份、概率、选择顺序、试次掩码和收据，重算最终NLL、核查冻结提名，
并实际执行完成后的resume以确认不新增PF，再生成三人汇总报告。
运行错误或科学未解决项都照实保留，不自动加预算、重开拟合或扩到其他被试。

## 怎样接回后续计划

原九人结果与87人准备包保留。本轮三人即使算完也不自动判为稳定心理参数，仍分别报告搜索、评分、挑战和边界问题。
完成且核对兼容性后，可将这三人纳入已完成记录，后续待拟合名单再更新为每条件28人、共84人。
不要直接运行旧87人脚本而重复计算本轮三人。全体、状态输出、恢复和exp4/exp5均不在本次运行范围。
