# 粒子滤波评价

本包包含粒子边际推理专用的汇总与诊断：状态和 ESS 汇总、策略审计、因果残差检查以及
选择传递反事实分析。这些模块只读取已经完成的结果，或执行用户明确要求的审计重跑；
它们不定义推理行为。

包级公开名称通过 `__getattr__` 按需加载。仅导入
`src.Bayesian_state.evaluation.particle_filter` 不会立即导入 matplotlib 或各审计实现；访问某个
具体函数时才加载对应模块。
