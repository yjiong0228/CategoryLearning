# Model 0923：由行为与口述约束的最简规则学习模型

版本：`model_0923`；当前可执行候选：`B0`；机制规范：`0923-B0-v1`。

0923 是待检验的新版本。0826 的代码路径、配置、文稿和既有结果均保留；
目前不能声称 0923 更好，也没有因改名而将 0826 的拟合参数当作 0923 的估计。

## 1. 本轮交付与尚未完成的部分

目前完成最简行为核心、三个 condition 的配置、选择后口述的联合概率接口，
以及 12 人的保守口述结构审查。已建立由目录生成、允许省略和模糊躯干参照的
候选报告概率矩阵；真实文本中仍有未覆盖内容，报告参数尚未校准。
**没有进行真实口述联合拟合，没有拟合 0923 被试参数，没有选出最终机制组合。**

当前行为核心继续使用既有 29 条二分类 / 116 条四分类目录及其功能相似度。
它是用于验证过程的 B0 起点，不是已通过口述审查的新规则空间。
原 `report_review.csv` 保留初始 `unreviewed` 状态；新增审查见第 10 节。
编码有效不等于目录覆盖，自动语法覆盖也不等于被试实际使用了那条规则。

## 2. 需要解释的现象

快速改善、缓慢渐进、长期停滞后明显改善是预测目标，不是输入模型的被试身份。
学习速度和曲线形状分别记录，不强行把每个人分入三个互斥组。
一次达到正确率标准也不等于以后永久掌握。

| 现象 | B0 可检验的解释 | 需要口述补充的信息 |
|---|---|---|
| rapid | 较早考虑到有效规则，证据较快集中 | 是否较早形成稳定表述；是否采用目标以外的有效规则 |
| slow and gradual | 候选支持缓慢变化或候选逐步修订 | 内容持续改变，还是内容稳定而使用逐渐准确 |
| slow but abrupt | 晚发现有效规则，或已有候选的支持随后占优 | 新口述是否随行为一起出现，还是早已出现相关表述 |

S122/S206/S215 是既有行为规则选出的开发案例，不据其模型轨迹指定解释。
全部 12 人（每 condition 四人）均保留，包括不明确偏向渐进或阶跃的记录。
既有分类未控制全部刺激难度与时间依赖，不能直接等同于认知类型。

## 3. B0 的逐试次过程

可理解为：保留少量解释 → 用已有反馈决定是否修订 → 根据当前支持作答 →
报告本题的理由 → 接收反馈 → 更新证据。

### 3.1 有限候选与证据

工作空间 $A_t$ 保存 $M_s$ 个候选；$\pi_t^-(h)$ 是当前选择前对候选的相对支持。
为减少第一步改动，仍沿用固定满员空间和均匀初始候选抽样。
这不意味着已测得真实工作记忆容量，也未证明简单规则和复杂规则认知成本相同。

每个活跃规则依据知觉刺激产生类别概率 $p_h(y\mid x_t)$。知觉噪声沿用学习前的
被试知觉测量。规则精度固定为 $\beta_{0,s}$，不在 B0 中随反馈变化。

反馈后保留一条证据记忆：

\[
\pi_t^+(h)\propto[\pi_t^-(h)]^{\gamma_s}L_t(h),\qquad 0\leq\gamma_s\leq1.
\]

反馈增益为 1，静态永久记忆权重为 0。Condition 3 对规则与未知配对的联合分布
使用相同的遗忘原则，然后取规则边际；不另设配对遗忘参数。

### 3.2 统一的失败控制

只读取已经完成的反馈，且不在 session 边界暗中重置状态：

\[
F_t=0.6F_{t-1}+0.4(1-r_{t-1}),\qquad F_1=0,
\]

\[
E_t=\operatorname{sigmoid}\{\operatorname{logit}(E_{0,s})+a_s F_t\},
\qquad a_s\geq0.
\]

两版本后续比较均先固定 $\delta_F=0.6$。0923 不再分别估计正确后和错误后的基线。
$E_{0,s}$ 是没有失败积累时的基线，$a_s$ 是失败的推动程度。
即使 $a_s=0$，诊断仍保存正确的 $F_t$，但它不影响选择或搜索。

对 condition 3，控制器直接使用原分值：错误、部分正确、完全正确分别贡献
1、0.5、0 的失败量。这是“按得分损失加权”的简洁假设，**不是信息量或惊讶度**。
它有别于 0826 控制器将 0 和 0.5 都作为非完全成功；原三值反馈另用于精确证据更新。
不预先规定部分正确反馈一定保护哪一层规则。

第一题只初始化工作空间，没有替换事件，实际替换概率记为 0；$E_0$ 从后续题起生效。

### 3.3 每次最多修订一个候选

对 $t\geq2$：

\[
K_t\sim\operatorname{Bernoulli}(E_t),\qquad K_t\in\{0,1\}.
\]

没有第二次独立搜索掷币，也不使用多槽位二项替换。
发生修订时，沿用权重 $1-\pi_{t-1}^+(h)+\epsilon$ 从旧候选中移走一条。
所有候选都允许移走；不存在受保护的执行槽位。

局部提议仍围绕整个旧信念分布，通过既有功能相似度生成；全局提议使用基础先验。
二者以固定于被试内的 $g_s$ 混合。局部宽度 $\tau_L=0.10$；B0 不让失败额外扩大范围。
新候选来自更新前不活跃的规则；“新”仅指当前不在空间内，允许在之后重新想到旧规则。

继承的功能相似度是暂定参照，不冒充口述语义或结构编辑距离。
相似度资源、标签偏好、目录条目数造成的先验偏好都需要在规则表示审查中核对。

### 3.4 不扰动保留规则的信念质量

无修订时，$\pi_t^-=\pi_{t-1}^+$。移走 $d$、补入 $u$ 时：

\[
\pi_t^-(h)=\pi_{t-1}^+(h)\ (h\text{ 为保留规则}),\qquad
\pi_t^-(u)=\pi_{t-1}^+(d).
\]

例如 $(0.89,0.10,0.01)$ 中第三条被替换，保留两条仍为 0.89 和 0.10，新人获得 0.01。
只保留一个新人时，共享的 mass-preserving transport 精确退化为这个公式。
它不使新人获得强支持，后续需要反馈积累；若妨碍数据中的快速发现，应作为明确不足检验。

Condition 3 同时保留旧规则的联合配对质量，新规则的条件配对分布沿用共享的
局部/全局投影初始化。这仍是一个迁移假设；$g_s$ 在该条件下也影响新人的配对初始化，
不能把它宣称为完全纯粹的搜索距离参数。旧规则的联合质量不因这个投影而重置。

### 3.5 作答无独立执行门槛

最小生成解释：本题按 $\pi_t^-$ 调用一个规则，并按该规则作答。
只拟合选择时，将本题规则边缘化：

\[
P(y_t\mid x_t,\mathcal D_{<t})=\sum_h\pi_t^-(h)p_h(y_t\mid x_t).
\]

已有候选每题都可以影响选择，不要求先引入新人，不使用固定 20% 切换事件。
B0 不保持独立的跨题执行状态；不是把混合选择概率解释为人能同时执行所有规则。
不增加输出 lapse、隐含阶段切换或单独掌握状态。

### 3.6 当前参数与固定约定

| 量 | 作用 | 当前示例配置 |
|---|---|---:|
| $M_s$ | 候选槽位数 | 3 |
| $\gamma_s$ | 旧证据保留 | 0.80 |
| $E_{0,s}$ | 基线修订概率 | 0.05 |
| $a_s$ | 失败推动修订 | 1.0 |
| $g_s$ | 全局提议混合比例 | 0.20 |
| $\beta_{0,s}$ | 固定规则精度 | 5.0 |

六个量是未来可估计的被试参数；表中数值只让实现可运行，不是拟合结果或最终参数范围。
$\delta_F=0.60$、$\tau_L=0.10$ 和 $\epsilon=10^{-12}$ 固定。
容量仍受共享空间“至少同样多非活跃候选”的实现约束；未新增大容量认知假设。
正式搜索范围、数值精度和恢复方案需在后续参数估计步骤冻结，不能把 smoke 默认值直接投入正式比较。

## 4. 三个 condition 共用什么、区别在哪里

| condition | 实验名称 | 必须保留的输入语义 |
|---|---|---|
| 1 | Task1 | 两分类、二值反馈 |
| 2 | Task3 | 四分类、二值反馈；无事先告知的层级知识 |
| 3 | Task2 | 四分类、0/0.5/1 反馈；已告知两科各两种，但未知反应配对 |

两个四分类任务的目标分类结构相同，反馈和事先知识不同。
学习算法不读取 rapid/gradual/abrupt 标签，也不能从真实 category 初始化正确规则或配对。
Condition 3 使用 `hierarchical_pairing` 和 `HierarchicalPairingMemoryModule`，
三个配对从均匀状态开始。`choice`、`presskey`、`category` 按既有数据契约区分。

## 5. 行为与口述的共同观测过程

实际顺序是“选择 → 口述 → 反馈”。令 $o_t$ 表示结构化报告代码，
$R(o\mid h,y,x)$ 是规则 $h$ 在当前刺激、所选类别下生成报告的概率，
允许省略未报告的分支和表达噪声。最小假设是同题行为和口述由同一被调用规则产生：

\[
P(y_t,o_t\mid x_t,\mathcal D_{<t})
=\sum_h\pi_t^-(h)p_h(y_t\mid x_t)R(o_t\mid h,y_t,x_t).
\]

观察选择后，预测报告应使用 $P(o_t\mid y_t,x_t,\mathcal D_{<t})$。
这不会让模型在预测本题选择时提前读取本题口述或反馈。
研究者根据选择对潜在规则重新加权，也不等于学习者从自己的选择得到新反馈。

`model/oral_observation.py::predict_choice_oral` 接收一个粒子内的规则权重、
规则类别概率和已归一化报告矩阵，返回联合分布与条件报告分布。
对多个粒子，应先计算各粒子内联合概率，再按同一时点的粒子权重平均；
不能把分别平均的行为概率和报告概率相乘。

接口与候选报告矩阵已可测试，**矩阵参数尚未校准，口述尚未接入 PF 的观测权重更新**。
现有编码器输出的 $P(h\mid o)$ 或兼容度不是 $P(o\mid h,y,x)$，不能仅重命名后使用。
也不能仅对每个实际观察到的口述做规则方向归一化，再声称获得了报告空间上的生成概率。

当前表示审查保存：原话、当前选择类别、特征顺序、关系/组合、可能省略的分支、
无法编码或目录外的内容及编码不确定性。未知报告不能强行归入目标规则。
报告噪声应由开发数据或独立编码一致性确定，不能为对齐模型轨迹反复调整。


### 5.1 躯干：物理长度明确，口述参照仍有不确定性

2026-09-23 经用户确认，物理躯干对应 0.75；部分被试事后解释，其口中“躯干”
指的心理参照可能稍长。记录这个信息，不据此给所有人或所有试次指定新阈值。

例如“躯干短于脖子、长于尾巴”编码为“脖子大于躯干参照、尾巴小于躯干参照”。
参照位置保持未定；不能把两项都编码为“大于”，也不能一律替换为 0.5 或 0.75。
旧解析器对一些躯干主语比较存在方向错误；0923 的独立编码修正这些明确方向，
不重写旧数据和旧结果。

初版在**口述观测层**使用共用模糊参照，不增加被试参数或移动行为规则边界。
只有真实选择也支持边界偏移，才另行评估行为规则的阈值；不能把口述不确定性
直接归因于学习机制或知觉噪声。个人参照偏移仅在反复报告支持稳定差异时检验。

### 5.2 从规则生成报告的最小候选

先从既有目录生成所有允许的报告，再查看实际原话：

1. 取规则在所选类别中的一个几何分量；多分量暂作等权。
2. 对分量中的每个关系，以共用概率 $q$ 说出，以 $1-q$ 省略。
3. 单特征分界允许使用普通长短表达或“躯干”表达。分界与共用参照中心越接近，
   “躯干”表达的概率越高，随差距连续下降；比较方向始终保留。
4. 保留显式的空内容 `EMPTY` 和目录语法外内容 `OTHER`。缺失记录和未解析文本
   均保持独立状态，不自动作为这两种生成事件。

具体概率、限制和例子见[口述表示说明](model_0923_oral.md)。
`model/oral_report_space.py` 返回规范化的 $R[H,C,O]$，可接入前述联合概率接口。
二分类有 78 个、四分类有 410 个报告代码，包括两个特殊代码；三个 condition
共用编码原则和观测参数，词表不由实际报告频数决定。

这仍是候选观测模型：它暂不依赖当前刺激来生成额外描述，不处理所有同义改写，
也没有校准参照宽度、表达率或省略率。`OTHER` 只汇总语法外内容，不能预测
具体的新规则。当前结果不够支持直接进入正式的行为与口述联合模型选择。

### 5.3 表示修订的范围约束（2026-09-23 用户确认）

取消四分类的 24 种标签全排列方案。标签编号变化不作为新增规则候选，
也不以延迟枚举、槽位内排列分布等方式保留整套 24 种映射。
当前 B0 仍使用原固定标签；这里记录后续设计约束，不声称标签缺口已经解决。

已接受的后续方向是：在有限目录内允许同一特征参与多个判断；
区分规则内容与额外刺激描述；核实物理尺度后单独检验明确的加权和运算。
各项逐步检验，不能把每种口述组合都作为新规则加入目录。

标签问题的轻量候选是在活跃划分结构内维护“区域到反应类别”的关联权重，
结构目录不因标签扩张。关联权重是根据历史反馈更新的状态，不是逐题自由拟合参数。
这是待定义、待检验的替代方向，尚未采用或实现。
若独立维护各区域的类别关联，就不再严格保证一一对应，可能允许学习早期多个区域
暂时指向同一类别；不能把它称为对 24 种排列的等价计算简化。
必须先明确二值/三值反馈下的更新与现有配对状态的关系，禁止读取真实类别初始化，
也不能根据本题口述临时重标来消除失配。

## 6. 选择性增加的机制及加入条件

| 候选版本 | 只有哪类不足支持加入 | 当前状态 |
|---|---|---|
| `B1_structural_revision` | 稳定的局部特征/运算/分支修订不能由功能相似度预测 | 未实现；先做口述结构审查 |
| `B2_execution_inertia` | 控制证据后仍有额外的规则使用持续性或切换迟滞 | 未实现；不默认给 abrupt 被试添加 |
| `B3_precision_learning` | 可比较刺激上口述内容稳定而准确率继续改善 | 未实现；不能仅据口述重复就认定规则完全不变 |
| `B4_directed_hierarchy` | 正确的三值反馈更新仍解释不了保留一层、修改另一层 | 未实现；不向模型泄漏正确配对 |

执行惯性的候选可让“继续使用上次规则”获得额外偏好，但切换仍每题可发生，
不再与新人出现绑定。结构化修订先作为现有局部提议的替代，避免不必要地叠加两套近邻核。
不同扩展先单独检查；只有发现必要的交互才组合，不一次扫描所有机制组合。
当前配置守卫只接受 `B0`，不会将尚未实现的候选标签当作可运行模型。

## 7. 采用模型的证据规则

1. 先列出基础模型稳定漏掉的具体行为/口述现象；不能因潜在曲线更好看而加机制。
2. 用真实逐题选择与报告预测评价，不用新旧控制曲线的相似程度决定版本。
3. 两个观测通道分别评分。某项改善、另一项实质下降属于权衡，不能靠合并总分隐藏。
4. 控制数据、计分试次、规则表示、可用历史、优化预算与数值误差。
   B0 同时改变几项结构，若整体有改善，还需针对关键机制做单因素桥接比较后归因。
5. 预留时序后段或新被试；过去的真实反馈可以按协议在线更新状态，未来反馈不能用于当前预测。
   用于设计的 12 人为开发样本，不能把其口述重新称作未参与开发的独立验证。
6. 按被试和 condition 报告模型间改变量，避免长记录和四分类任务凭试次数/分值尺度主导结果。
   同时报告学习速度与变化形状，不以表现标签反向选择一套专属机制。
7. 完成参数/模型区分检查与适当数值校准；自主生成用于检查模型是否能产生所需学习过程。
   重复种子只反映计算随机性，不增加人群样本量。
8. 没有可靠改善则保留已有版本；完整拟合和最终机制选定不属于本轮实现检查。

若词汇/规则库改变，旧版在同一表示下的对照应另命名并记录，不能改写冻结的 0826。

## 8. 当前代码、审查产物与运行方式

- 核心：`model/modules/hypothesis_transition/unified_rule_search.py`；复用工作空间生命周期。
- 版本契约：`model/model_0923.py`，由 `ModelConfig` 自动检查。
- 报告接口：`model/oral_observation.py`；候选矩阵由 `model/oral_report_space.py` 提供，待校准。
- 保守口述编码：`evaluation/oral/structure_0923.py`；不改写旧编码。
- 三条件配置：`configs/exp123/model_struct/model_0923_cond{1,2,3}_B0.yaml`。
- 定向测试：`tests/bayesian_state/test_model_0923.py`。
- 口述准备：`workflows/analysis/prepare_model_0923.py`，只读来源、拒绝覆盖输出。

从仓库根目录运行口述准备：

```bash
python -m src.Bayesian_state.workflows.analysis.prepare_model_0923 \
  --cohort-config CategoryLearning_codes/figures/fig3/twelve_subject_config.json \
  --analysis-dir results/model_0826/fig34_twelve_subjects_20260921_v1/base_analysis \
  --behavior-summary results/model_0826/fig34_twelve_subjects_20260921_v1/analysis/subjects.csv \
  --output-dir results/model_0923/report_inventory_NEW
```

每个小规模配置只运行一个被试、前 32 题、8 个粒子、一次重复和一个 worker：

```bash
OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 \
python -m src.Bayesian_state.run_simulation \
  --config configs/exp123/simulation_cfg/model0923_cond1_B0_smoke.yaml
```

替换配置中的 condition 可分别检查 S122、S206、S314；重跑前使用新的输出目录。
这三个记录是工程检查，不代表三个认知类型的拟合结果。
旧日志中的 `predictive_m` 等槽位率字段只作为继承的兼容诊断保留；
0923 的实际计数分布是 `single_candidate_bernoulli`，搜索概率读取 `swap_probability`。

## 9. 2026-09-23 本轮验证记录

- 202 项定向测试通过：0923 控制/迁移/口述接口、0826 版本契约、共享工作空间、
  condition 3 配对记忆/推理/自主生成，以及期刊迁移前保存的独立数值基准。
- 三个真实短序列均完成：S122 / S206 / S314，各前 32 题、8 粒子、1 次重复、1 worker。
  预测为有限且归一化的 2/4/4 类概率；每题替换数量的边际均值不超过 1，候选数保持为 3。
- 独立用原反馈重算失败量与搜索概率，最大绝对误差不超过 $2.3\times10^{-16}$。
  Condition 3 的检查实际包含 0、0.5、1 三种反馈。
- 口述准备保留 12 人 7,936 题：7,612 条原有效编码，沿用共同计分 mask 后 7,600 条；
  2,539 种 condition × choice × 原话组合。它们均待规则覆盖审查，不等于 2,539 条认知规则。
- 实现检查记录：`results/model_0923/implementation_check_20260923_v1/`。
  口述清单：`results/model_0923/report_inventory_20260923_v2/`。

这些结果验证实现和数据通路，不验证参数可恢复性、真实口述似然或 0923 优于 0826。
这一阶段后的口述审查进展见下节；有界拟合比较须在表示与报告参数校准后进行。


## 10. 2026-09-23 口述结构审查进展

结果位于 `results/model_0923/report_structure_20260923_v2/`。
12 人 7,936 题全部保留；按原话、condition、choice 和特征顺序拆分为 2,647 种非空记录。
原清单的 2,539 种组合没有区分特征顺序，因此不能直接作为规则代码共用。

| 自动审查结论 | 试次数 | 解释边界 |
|---|---:|---|
| 完整类别分量 | 2,356 | 不等于完整分类规则 |
| 可由省略生成 | 377 | 可同时对应多条完整规则 |
| 可由模糊躯干表达生成 | 587 | 只表示语法允许；概率取决于未校准参数 |
| 只能在其他类别标签找到描述 | 1,497 | 不自动重标，需核对固定标签限制 |
| 生成语法未覆盖 | 1,685 | 可能是额外描述、冗余关系或结构缺失 |
| 待复核 | 1,251 | 包括部分解析、歧义、矛盾和数值尺度未定 |
| 缺失 | 183 | 不作为认知省略事件 |

- 698 题的明确躯干比较与旧解析方向不一致，均来自 S314。不能继续用原对齐值解释其学习机制。
- S122 的 74 题加权和报告保存了“四条腿”的系数 4。物理躯干位置已知仍不足以
  单独确定所有长度的仿射变换；不能直接在归一化坐标里将“四条腿”改成一条。
- 三组演示宽度下六个矩阵均有限、非负且归一化，最大行和误差 $2.3\times10^{-16}$。
  宽度 0.05/0.10/0.20 仅检验敏感性，不是拟合值、推荐范围或心理测量结果。
- 238 项定向测试通过，包含新增 36 项口述层测试、0923 基线检查及 0826 的独立数值回归；CLI 帮助与真实 12 人审查入口均已执行。
- 这些是保守自动审查结果，仍需人工复核代表性案例；未拟合 0923 参数，未选择最终学习机制。

接下来先处理固定类别标签、额外刺激描述与真正结构缺失这三种不同问题，
并校准报告参数。所有版本使用相同且冻结的观测表示后，再进行有界拟合比较。
不能仅靠加宽口述噪声掩盖规则目录的缺口。
