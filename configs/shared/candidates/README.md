# Candidate configurations

Versioned, finite candidate libraries used by optimization live here. Each
subdirectory belongs to one model component and contains its own schema and
provenance notes. Runtime Python modules do not own these experiment resources.
